name: ci

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Webhook request
        run: |
          curl -X GET ${{ secrets.ENDPOINT_URL }}
        env:
          ENDPOINT_URL: ${{ secrets.EASYPANEL_STAGING_URL }}
